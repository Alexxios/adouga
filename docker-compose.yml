
services:
  # Your FastAPI App
  fastapi_app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/project/app
      - ./common:/project/common
